{
  "schema": 1,
  "description": "Understand the food item user consumed along with the quantity and return the approximate number of calories.",
  "execution_settings": {
    "default": {
      "max_tokens": 2000,
      "temperature": 0.5,
      "top_p": 0.5,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input": {
    "parameters": [
      {
        "name": "inputString",
        "description": "Plaintext input which describes user's query which is potentially going to tell the food item the user consumed and the quantity in weight or volume units."
      },
      {
        "name": "history",
        "description": "Conversation history"
      }
    ]
  }
}